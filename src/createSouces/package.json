{
    "name": "createsouces",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "invoke": "npm run build && stackery local invoke -e test --aws-profile rrp"
    },
    "devDependencies": {
        "aws-sdk": "~2",
        "@types/aws-lambda": "^8.10.46",
        "@types/aws-sdk": "^2.7.0",
        "@types/node": "^12.6.8",
        "typescript": "^3.5.3"
    }
}
